{
    "name": "bunzo",
    "version": "0.0.0",
    "description": "Wordpress Starter Theme using the REST-API with Redux and React",
    "config": {
        "vendor-dir": "lib/vendor"
    },
    "require": {
    },
    "extra": {
        "installer-paths": {
            "lib/vendor{$name}": ["vendor/package"]
        }
    },
    "autoload": {
        "psr-4": { "Bunzo\\": "lib/classes" }
    }
}
